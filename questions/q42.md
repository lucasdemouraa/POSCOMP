# Question 42

![Original question image](images/q-42.png)

**English transcription of the question:** Typically, the execution of a process in a time-sharing operating system is characterized by alternation between CPU execution cycles and I/O waiting cycles until completion. The average times of the CPU execution cycles and I/O waiting cycles will be called, respectively, `exec_t` and `es_t`. Consider a set of processes that follow this alternation pattern between CPU and I/O, which execute under an operating system with preemptive round-robin (RR) scheduling. Assume that the average time required to perform a context switch is given by `switch_t`. Select the alternative that best minimizes the average turnaround time of the processes.

A) (`exec_t > switch_t`) and (`exec_t > quantum`).  
B) (`exec_t < switch_t`) and (`exec_t < quantum`).  
C) (`exec_t > switch_t`) and (`exec_t < quantum`).  
D) (`exec_t < switch_t`) and (`exec_t > quantum`).  
E) (`exec_t + switch_t`) > quantum.

**Prompt:** Answer the question in this image by explaining step by step the reasoning used to answer it. Inform if the question is not clear or does not have a possible answer.
